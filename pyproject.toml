[project]
name = "overseas-japanese"
version = "0.1.0"
description = "FastAPI + Flask viewer for overseas Japanese population statistics"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "babel>=2.17.0",
    "fastapi>=0.128.0",
    "flask>=2.4.0",
    "pycountry>=24.6.1",
]
